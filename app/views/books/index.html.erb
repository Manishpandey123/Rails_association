<ul>
    <% @books.all.each do |f|%>

    <li><%= f.topic%></li>
    <li><%= f.author_id%></li>
    <%= link_to "Edit",  edit_book_path(f)%>
    <%= link_to "Delete",  book_path(f), method: :delete, data: {confirm: "are you sure?"}%>
    <%= link_to "Show", book_path(f) %>
    <%end%>
</ul>


 <%= link_to "New", new_book_path %>